package homework_1;

/*
1. Palindrome Number
   Given an integer x, return true if x is a palindrome, and false otherwise.
*/

public class Task_1 {

     /*
     1. Convert the given integer x to a string.
     2. Traverse the string from the left and right ends simultaneously until the pointers meet at the middle.
     3. If the characters at the current pointers are not the same, return False.
     4. If the loop completes without returning False, return True.

     1. Если число x меньше 0, вернуть false.
     2. Преобразовать число x в строку.
     3. Инициализировать два указателя, один указывающий на первый символ строки (левый указатель), а другой указывающий на последний символ строки (правый указатель).
     4. Если символы в текущих указателях не совпадают, вернуть False.
     5. Если цикл завершится без возврата False, верните True.

     */


}
